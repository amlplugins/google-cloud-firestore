/**
 * @amlplugins/google-cloud-firestore
 *
 * Thin re-export of @google-cloud/firestore (CommonJS `export =` SDK).
 *
 * Cloud Firestore.
 */

import _sdk from "@google-cloud/firestore";
export { _sdk as sdk };
export default _sdk;
